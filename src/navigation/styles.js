import styled from 'styled-components';

const Icon = styled.Image`
  width: 26px;
  height: 26px;
  resize-mode: contain;
`;

const BackIcon = styled.Image`
  resize-mode: contain;
`;

export { Icon, BackIcon };
